import { motion } from 'framer-motion'
import { getRank, MAX_XP, XP_RANKS } from '@/data/curriculum'
interface Props { xp:number; animated?:boolean }
export function XPBar({xp,animated=true}:Props){const rank=getRank(xp);const pct=Math.min(100,Math.round(xp/MAX_XP*100));const i=XP_RANKS.findIndex(r=>r.minXp===rank.minXp);const next=XP_RANKS[i+1];const toNext=next?next.minXp-xp:0;return <div className="w-full"><div className="flex justify-between items-center mb-1"><span className="text-xs font-mono font-semibold" style={{color:rank.color}}>{rank.title}</span><span className="text-xs text-gray-400 font-mono">{xp.toLocaleString()} XP{next&&<span className="text-gray-600"> · {toNext} to {next.title}</span>}</span></div><div role="progressbar" aria-label={`Experience: ${xp} of ${MAX_XP}`} aria-valuemin={0} aria-valuemax={MAX_XP} aria-valuenow={xp} className="h-2 bg-gray-800 rounded-full overflow-hidden"><motion.div className="h-full rounded-full" style={{background:rank.color}} initial={animated?{width:0}:{width:`${pct}%`}} animate={{width:`${pct}%`}} transition={{duration:.8,ease:'easeOut'}}/></div></div>}
